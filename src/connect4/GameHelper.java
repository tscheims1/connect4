package connect4;

public class GameHelper {

	/**
	 * Check if the a Player win
	 * Return the Number of the Player who wins the game
	 * or 0 (noboy wins)
	 * @param board
	 * @return
	 */
	public static int checkWinCondition(int[][] board)
	{
		for(int y =0; y < Game.ROWS;y++)
		{
			
		}
		return 0;
	}
}
